---
title: Selesai
description: An extension-first coding agent with subagents, durable workflows, research, recovery, and token-conscious tooling built in.
template: splash
hero:
  title: "The coding agent<br><span class='accent'>built to finish.</span>"
  tagline: "Selesai turns a minimal coding harness into a complete workspace: delegate work, research the web, recover context, and ship without assembling a plugin stack first."
  actions:
    - text: Get started
      link: get-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/SelesaiInTech/selesai-code
      icon: external
      variant: secondary
  image:
    html: |
      <div class="terminal-card" aria-hidden="true">
        <div class="terminal-card__bar"><i></i><i></i><i></i><span>selesai — main</span></div>
        <div class="terminal-card__body">
          <p><span class="prompt">❯</span> ship this feature safely</p>
          <p class="muted">  Tracing codebase...</p>
          <p>  ↗ delegated 3 focused checks</p>
          <p>  ↺ checkpoint created</p>
          <p>  ✓ implementation verified</p>
          <p><span class="success">Done.</span> 12 files · 48 tests passed</p>
        </div>
      </div>
---

import InstallCommand from "../../components/InstallCommand.astro";
import CapabilityCard from "../../components/CapabilityCard.astro";
import { capabilities } from "../../data/capabilities.ts";

<div className="trust-strip" aria-label="Project highlights">
  <span>extension-first</span>
  <span>in-tree tooling</span>
  <span>bilingual docs</span>
  <span>open source</span>
  <span>npm: @selesai/code</span>
</div>

<section className="home-section">
  <div className="home-section__heading">
    <p className="eyebrow">Start in one command</p>
    <h2>No plugin scavenger hunt.</h2>
    <p>Install one maintained package. Get the coordinated tools, skills, and terminal experience Selesai ships and tests together.</p>
  </div>
  <div className="install-panel">
    <InstallCommand command="npm install -g @selesai/code" />
    <InstallCommand command="selesai" />
  </div>
</section>

<section className="home-section">
  <div className="home-section__heading">
    <p className="eyebrow">A complete working loop</p>
    <h2>More than chat in a terminal.</h2>
    <p>Delegate deep work, bring in external evidence, preserve session continuity, and keep token spend visible.</p>
  </div>
  <div className="capability-grid">
    {capabilities.slice(0, 6).map((capability) => (
      <CapabilityCard slug={capability.slug} locale="en" />
    ))}
  </div>
  <a className="home-link" href="capabilities/">Explore every capability <span aria-hidden="true">→</span></a>
</section>
