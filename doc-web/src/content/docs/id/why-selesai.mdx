---
draft: false
title: Mengapa Selesai
---

import ComparisonCallout from "../../../components/ComparisonCallout.astro";
import SourceEvidence from "../../../components/SourceEvidence.astro";
import { categories, capabilities } from "../../../data/capabilities.ts";

Selesai adalah fork dari [Pi coding agent](https://github.com/earendil-works/pi). Selesai mempertahankan inti terminal-first yang dapat di-*hack* dan menambahkan satu set extension, skill, serta perbaikan ruang kerja yang terkoordinasi. Halaman ini memisahkan apa yang sudah disediakan Pi dari apa yang ditambahkan atau dibundel Selesai, serta menautkan setiap klaim material Selesai ke bukti.

## Apa yang sudah dilakukan Pi

Pi tetap menjadi harness coding kecil dan mumpuni. Selesai mempertahankan kemampuan dasar ini:

- coding interaktif terminal-first dengan `read`, `bash`, `edit`, `write`, `grep`, `find`, dan `ls`;
- beberapa model provider, API key, OAuth, thinking level, gambar, dan retry;
- sesi persisten dengan resume, fork, navigasi tree, compaction, dan export;
- mode print, JSON event-stream, dan RPC;
- extension TypeScript, skill, template prompt, tema, package, dan SDK Node.js yang dapat di-*embed*;
- project trust dan resource loading yang dapat dikonfigurasi.

<ComparisonCallout
  claim="Selesai tidak menghapus inti Pi. Selesai dibangun di atas harness coding interaktif yang sama."
  evidence={[
    { text: "Selesai README — inti kompatibel Pi", url: "https://github.com/SelesaiInTech/selesai-code/blob/main/README.md" },
    { text: "Repositori Pi", url: "https://github.com/earendil-works/pi" }
  ]}
/>

## Perbandingan sekilas

Pi memiliki ekosistem extension dan skill opsional yang luas. Taruhan Selesai berbeda: instal satu package dan dapatkan satu set **{capabilities.length} kemampuan** yang terkoordinasi — {capabilities.filter((capability) => capability.distribution === "bundled").length} ekstensi bundel yang dimuat otomatis, {capabilities.filter((capability) => capability.distribution === "optional").length} ekstensi opsional, dan {capabilities.filter((capability) => capability.distribution === "core").length} built-in inti — diuji bersama dan dikirim dengan setiap rilis.

<ComparisonCallout
  claim="Pi memiliki ekosistem; Selesai membundel satu set yang terkoordinasi dan teruji. Setiap kemampuan bundel berada in-tree, di-versioning bersama rilis, dan disertai panduan serta bukti source."
  evidence={[
    { text: "Manifest ekstensi bundel", url: "https://github.com/SelesaiInTech/selesai-code/blob/main/src/extensions/package.json" },
    { text: "Repositori Pi", url: "https://github.com/earendil-works/pi" }
  ]}
/>

## Bundel sekilas

Setiap kemampuan bundel, dikelompokkan berdasarkan kategori:

{categories.map((category) => (
  <section className="category-section" id={category.id}>
    <h3>{category.indonesian}</h3>
    <ul>
      {capabilities
        .filter((capability) => capability.category === category.id && capability.distribution === "bundled")
        .map((capability) => (
          <li>
            <strong>{capability.id.displayName}</strong> — {capability.id.benefit}
          </li>
        ))}
    </ul>
  </section>
))}

## Apa yang ditambahkan atau dibundel Selesai

Daftar lengkap: semua {capabilities.length} kemampuan yang dikelompokkan berdasarkan kategori, masing-masing menautkan ke panduannya. Ekstensi bundel dimuat otomatis; ekstensi opsional dan built-in inti ditandai.

{categories.map((category) => (
  <section className="category-section" id={category.id}>
    <h3>{category.indonesian}</h3>
    <table>
      <thead>
        <tr>
          <th>Kemampuan</th>
          <th>Panduan</th>
        </tr>
      </thead>
      <tbody>
        {capabilities
          .filter((capability) => capability.category === category.id)
          .map((capability) => (
            <tr>
              <td>
                {capability.id.displayName}
                {capability.distribution === "optional" && <em> (opsional)</em>}
                {capability.distribution === "core" && <em> (inti)</em>}
              </td>
              <td>
                <a href={`${import.meta.env.BASE_URL}id/${capability.guideRoute}/`}>Baca panduan</a>
              </td>
            </tr>
          ))}
      </tbody>
    </table>
  </section>
))}

## Mengapa *bundling* penting

Pi memiliki ekosistem extension yang luas. Memasang banyak package terpisah berarti harus melacak pemeliharaan, kompatibilitas, dan batas supply-chain-nya. Selesai membawa extension yang mendefinisikan pengalaman Selesai di `src/extensions/`, mengujinya bersama aplikasi, dan mengirimkannya bersama setiap rilis.

Ini membuat source tetap terlihat, dapat direview, dan diaudit di satu tempat. Ini tidak menghilangkan risiko supply-chain — Selesai masih memiliki runtime dependencies — tetapi menghilangkan kebutuhan untuk memasang banyak extension tidak terkait sebelum Selesai menjadi berguna.

<ComparisonCallout
  claim="Selesai adalah fork terkoordinasi, bukan installer plugin yang longgar. Kemampuan inti berada in-tree, di-versioning, dan diuji bersama."
  evidence={[
    { text: "Mengapa extension dibundel", url: "https://github.com/SelesaiInTech/selesai-code/blob/main/README.md" }
  ]}
/>

## Hasil praktis

- **Lebih sedikit gesekan setup:** instal `@selesai/code` dan dapatkan subagent, workflow, riset web, pertanyaan interaktif, pencarian kode, tool recovery, skill, dan UI yang lebih kaya dalam satu package — tanpa berburu plugin.
- **Pengeluaran token lebih kecil:** `/handoff-new`, pengingat konteks, rewrite RTK, pelacak TPS, dan permukaan compact Zentui menjaga konteks tetap kecil, tenang, dan fokus.
- **Pemulihan lebih baik:** checkpoint rewind berbasis git, undo per turn, handoff sesi, dan koordinasi antar sesi mengurangi biaya kesalahan.
- **Lebih sedikit kejutan:** setiap kemampuan dikirim dengan panduan, referensi konfigurasi, dan bukti source — tanpa plugin kotak hitam.

<SourceEvidence slug="pi-subagents" />
