---
draft: false
title: Capabilities
description: Explore the tools, workflows, recovery features, and productivity improvements bundled with Selesai.
---

import CapabilityList from "../../components/CapabilityList.astro";
import { categories, capabilities } from "../../data/capabilities.ts";

<div className="home-section__heading capabilities-intro">
  <p className="eyebrow">{capabilities.length} capabilities, one install</p>
  <h2>Everything Selesai ships, tested together.</h2>
  <p>
    `@selesai/code` installs <strong>{capabilities.filter((capability) => capability.distribution === "bundled").length} bundled extensions</strong> that load automatically, plus {capabilities.filter((capability) => capability.distribution === "optional").length} optional extension and {capabilities.filter((capability) => capability.distribution === "core").length} core built-in. Optional extensions are listed separately at the bottom of the page; core built-ins are also covered on the [Customization](../customization/) page. Every entry below links to a guide with examples, configuration, limits, and source evidence.
  </p>
</div>

<div className="comparison-legend">
  <h2>What the badges mean</h2>
  <p>Each capability carries a badge that says how it relates to the Pi baseline:</p>
  <ul>
    <li><span className="legend-badge legend-badge--difference">Selesai difference</span> Core differentiators — not part of Pi's baseline; Selesai builds them in-tree.</li>
    <li><span className="legend-badge legend-badge--bundled">Bundled</span> Shipped and maintained by Selesai; similar ideas may exist in Pi's ecosystem as separate packages.</li>
    <li><span className="legend-badge legend-badge--ergonomics">Ergonomics</span> Incremental quality-of-life improvements on the shared Pi baseline.</li>
  </ul>
</div>

{categories.map((category) => (
  <section className="category-section" id={category.id}>
    <h2>{category.en}</h2>
    <CapabilityList category={category.id} locale="en" />
  </section>
))}

{capabilities.some((c) => c.distribution === "core") && (
  <section className="category-section" id="core-extensions">
    <h2>Core built-in extensions</h2>
    <p>These capabilities are part of the Selesai runtime and are not listed in the extension manifest. See the <a href={`${import.meta.env.BASE_URL}customization/`}>Customization</a> page for a complete overview.</p>
    {categories.map((category) => (
      <CapabilityList category={category.id} locale="en" distribution="core" />
    ))}
  </section>
)}
{capabilities.some((c) => c.distribution === "optional") && (
  <section className="category-section" id="optional-extensions">
    <h2>Optional extensions</h2>
    <p>These extensions are available but not loaded automatically. Install or enable them separately before use.</p>
    {categories.map((category) => (
      <CapabilityList category={category.id} locale="en" distribution="optional" />
    ))}
  </section>
)}

