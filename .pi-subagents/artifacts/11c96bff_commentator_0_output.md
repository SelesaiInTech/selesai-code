Subagent exceeded turn budget after 22 assistant turns (soft limit 20 + grace 2).

Partial output before turn-budget abort:
 I'll start by exploring the repository structure and identifying the uncommitted changes.